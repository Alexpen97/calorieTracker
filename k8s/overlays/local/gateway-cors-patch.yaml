apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: nutritrack
spec:
  template:
    spec:
      containers:
        - name: gateway
          env:
            - name: CORS_ALLOWED_ORIGINS
              value: http://localhost:8088,http://localhost:5173,http://localhost,https://localhost
